import { dashboard, students, classes } from "./dashboard";

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, classes, students],
};

export default menuItems;
